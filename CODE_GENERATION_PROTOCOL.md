# LMT Code Generation & Implementation Protocol

## 目的
「とりあえず動くコード」ではなく、  
人間が目的・構造・変更理由を追跡でき、将来修正可能なコードを生成・実装するための運用プロトコル。

LMTの観点から、目的と構造を保持し、AIに説明責任を持たせ、変更履歴から監査可能にすることを最優先とする。

---

## コア方針

1. **目的優先**  
   実装前に「このコードが何を解決するのか」を1〜3文で明確にする。

2. **混同禁止**  
   目的 / 仕様 / 実装手段を分けて扱う。推測や不明点は隠さず明記する。

3. **最小構成から**  
   一度に大規模な変更をせず、小さな単位で実装・確認する。

4. **説明責任**  
   「なぜこの構造にしたか」「なぜ動くのか」を残す。説明できない処理を作らない。

5. **視点分離**  
   可能であれば、実装AIとテスト設計・レビューAIを分ける。

6. **人間ゲート**  
   権限外・危険・不可逆な操作は、人間の確認なしに行わない。

---

## 出力フォーマット（必須）

毎回、以下の順番で出力すること。

1. **今回の目的**（1〜3文）
2. **前提と未確定事項**
3. **実装方針**（なぜこの構造にしたか）
4. **変更するファイルと影響範囲**
5. **コード**
6. **テスト内容**（正常系・異常系・境界値・欠損・重複）
7. **実行方法**
8. **想定される失敗と復旧方法**
9. **技術的負債・今後改善すべき点**
10. **人間が最終確認すべき事項**

---

## 実装時の品質基準

- 同じロジックを重複させず、共通化を検討する
- 関数は一つの責任に絞り、長大化した場合は分割する
- 関数名・変数名と実際の役割を一致させる
- 深い条件分岐を避け、早期returnや処理分割を検討する
- 使用されていないコードやコメントアウトされた死体コードを残さない
- 「触るな」「なぜか動く」で済ませず、理由を確認・説明する
- 同じ処理を複数の流儀で実装しない
- 外部入力、失敗時、空データ、重複実行を考慮する
- ログやエラー文は、人間が原因を追跡できる内容にする

---

## テストの原則

- 正常系だけでなく、異常系・境界値・欠損・重複をテストする
- テストが実装の内部構造だけをなぞっていないか確認する
- 可能なら、コードを書いた視点とは別の視点で仕様からテストを作る
- テストが通ったことと、目的が満たされたことを分けて評価する

---

## 実装後のセルフレビュー観点

- 重複した処理はないか
- 関数の責任が多すぎないか
- 名前と処理が一致しているか
- 条件分岐が深すぎないか
- 不要なコードが残っていないか
- 説明できない処理がないか
- テストが実装と同じ思い込みを共有していないか
- 変更によって別の機能を壊す可能性はないか
- 人間が後から修正できる状態か

---

## 記録の残し方（推奨）

変更を行う際は、可能な範囲で以下を残す：

- なぜ作ったか / なぜ変えたか
- 何を変更したか
- どのファイルに影響するか
- 何をテストしたか
- 何がまだ不明か
- 元に戻す方法

GitHubでは、Issue・コミットメッセージ・このプロトコル文書に分けて記録する。

---

## 運用メモ

- しほちゃん自身がコードを読んで細部を判断する必要はない
- 目的と構造を保持し、AIに説明責任を持たせ、変更履歴から監査する運用を優先する
- プロンプト単体ではなく、「生成 → 説明 → 点検 → テスト → 修正」を一連の手順として扱う
